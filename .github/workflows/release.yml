name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build binaries
        run: |
          LDFLAGS="-s -w"
          TARGETS=(
            "darwin/amd64"
            "darwin/arm64"
            "linux/amd64"
            "linux/arm64"
            "windows/amd64"
          )
          mkdir -p skills/xlsx-scope/scripts
          for TARGET in "${TARGETS[@]}"; do
            GOOS="${TARGET%/*}"
            GOARCH="${TARGET#*/}"
            OUTPUT="skills/xlsx-scope/scripts/xlsx-scope-${GOOS}-${GOARCH}"
            if [ "$GOOS" = "windows" ]; then
              OUTPUT="${OUTPUT}.exe"
            fi
            echo "Building ${OUTPUT}..."
            CGO_ENABLED=0 GOOS="$GOOS" GOARCH="$GOARCH" \
              go build -ldflags="$LDFLAGS" -o "$OUTPUT" ./cmd/xlsx-scope
          done

      - name: Create zip
        run: |
          cd skills/xlsx-scope
          zip -r "../../xlsx-scope-${GITHUB_REF_NAME}.zip" \
            SKILL.md \
            references/ \
            scripts/xlsx-scope \
            scripts/xlsx-scope-darwin-amd64 \
            scripts/xlsx-scope-darwin-arm64 \
            scripts/xlsx-scope-linux-amd64 \
            scripts/xlsx-scope-linux-arm64 \
            scripts/xlsx-scope-windows-amd64.exe

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: "xlsx-scope-${{ github.ref_name }}.zip"
          generate_release_notes: true
